# 📚 Daily Learning Progress — August 10, 2026

## 🌐 Web Development — JavaScript localStorag

### Topics Completed

- Learned what `localStorage` is.
- Practiced `localStorage.setItem()`.
- Practiced `localStorage.getItem()`.
- Practiced `localStorage.removeItem()`.
- Learned that JavaScript is case-sensitive.
- Learned how to store JavaScript objects using `JSON.stringify()`.
- Learned how to convert stored strings back into objects using `JSON.parse()`.
- Practiced saving and retrieving project data.

### 💻 Code Concepts

```javascript
localStorage.setItem("name", "Saumay");

localStorage.getItem("name");

localStorage.removeItem("name");

localStorage.setItem("project", JSON.stringify(project));

let data = JSON.parse(localStorage.getItem("project"));
